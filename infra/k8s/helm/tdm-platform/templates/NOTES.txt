tdm-platform ({{ .Chart.AppVersion }}) has been installed as release "{{ .Release.Name }}" in namespace "{{ .Release.Namespace }}".

Services deployed:
  - control-plane      ({{ include "tdm-platform.fullname" . }}-control-plane:{{ .Values.controlPlane.service.port }})
  - governance-service  ({{ include "tdm-platform.fullname" . }}-governance-service:{{ .Values.governanceService.service.port }})
  - frontend            ({{ include "tdm-platform.fullname" . }}-frontend:{{ .Values.frontend.service.port }})

Check readiness (the control plane's real, dependency-aware check —
control_plane.platform.readiness):

  kubectl port-forward svc/{{ include "tdm-platform.fullname" . }}-control-plane {{ .Values.controlPlane.service.port }}:{{ .Values.controlPlane.service.port }}
  curl http://127.0.0.1:{{ .Values.controlPlane.service.port }}/api/v1/ready

{{- if not .Values.ingress.enabled }}

No Ingress is enabled (ingress.enabled: false). See
docs/AZURE_PRODUCTION_DEPLOYMENT.md for a real Ingress example.
{{- end }}
